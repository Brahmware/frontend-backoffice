import { Card, styled } from '@mui/material'

const CardColumnFlexCC = styled(Card)({
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
    width: '100%'
})

export default CardColumnFlexCC