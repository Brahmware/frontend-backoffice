import { styled } from "@mui/material";
import { Box } from "@mui/system";

const FullWidthFullHeightBox = styled(Box)({
    width: '100%',
    height: '100%',
    position: 'relative'
});

export default FullWidthFullHeightBox;